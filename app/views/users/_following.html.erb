<% if current_user %>
  <% if current_user.following?(@user) %>
    <%= button_to 'No seguir', following_path(@user), method: :delete, remote: true %>
  <% else %>
    <%= button_to "Seguir", followings_path(id: @user), remote: true %>
  <% end %>
<% end %>
